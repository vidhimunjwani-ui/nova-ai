{
  "name": "chat-gpt",
  "private": true,
  "scripts": {
    "dev": "npm --prefix my-app exec -- next dev my-app",
    "build": "npm --prefix my-app exec -- next build my-app",
    "start": "npm --prefix my-app exec -- next start my-app",
    "lint": "npm --prefix my-app exec -- next lint my-app"
  }
}
