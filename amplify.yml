version: 1
runtime-versions:
  nodejs: 20
frontend:
  phases:
    preBuild:
      commands:
        - cd my-app && npm ci
    build:
      commands:
        - cd my-app && npm run build
  artifacts:
    baseDirectory: my-app
    files:
      - '.next/standalone/**/*'
      - '.next/static/**/*'
      - 'package.json'
      - 'package-lock.json'
      - 'next.config.js'
      - 'next-env.d.ts'
      - 'app/**/*'
      - 'components/**/*'
      - 'lib/**/*'
      - 'hooks/**/*'
      - 'public/**/*'
  cache:
    paths:
      - node_modules/**/*
      - my-app/node_modules/**/*
      - my-app/.next/cache/**/*
