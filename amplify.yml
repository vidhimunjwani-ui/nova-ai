version: 1
runtime-versions:
  nodejs: 20
frontend:
  phases:
    preBuild:
      commands:
        - cd my-app && npm ci
    build:
      commands:
        - cd my-app && npm run build
  artifacts:
    baseDirectory: my-app/.next/standalone
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - my-app/node_modules/**/*
      - my-app/.next/cache/**/*
